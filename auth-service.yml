server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: rootaccount

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: mysecretkey
